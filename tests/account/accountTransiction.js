// create a solution to mantain a bank account's balance

// implement the Accaount class that manages an account's balance.
// the class has the following constructor and methods:

// 1_ the constructor Account(balance) where parameter balance denotes the initial balance of the account

// 2_ the method debit(amount) debits the amount from the account and returns true. 
//    if insuficent balance, do not debit and returns false.

// 3_ the methods getBalance() returns the current balance

// 4_ the methods credit(amount) credits the amount to the account

const nr = -14;
const toStringArray = nr.toString().split("-")

// const r = nr.toString()
// const s = r.split("-")

console.log(toStringArray)

console.log(typeof(toStringArray[0]))
console.log(toStringArray[0])

let p = parseInt(toStringArray[0])
console.log(p)